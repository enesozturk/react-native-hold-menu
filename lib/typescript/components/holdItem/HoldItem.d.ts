import React from 'react';
import type { HoldItemProps } from './types';
declare const HoldItem: React.MemoExoticComponent<({ items, bottom, containerStyles, disableMove, menuAnchorPosition, activateOn, hapticFeedback, actionParams, closeOnTap, children, }: HoldItemProps) => JSX.Element>;
export default HoldItem;
